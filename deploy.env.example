# Copy to deploy.env and fill in (never commit deploy.env)
# Used by: ./scripts/deploy-cloud.sh

# Public API URL (Render, Railway, ECS, etc.) — no trailing slash
API_URL=https://stream-catalog.onrender.com

# AWS Amplify app (Console → App settings → General)
AMPLIFY_APP_ID=dxxxxxxxx

# Branch to configure
AMPLIFY_BRANCH=main

# Used when configuring Render manually (not needed for deploy-cloud.sh)
DATABASE_URL=postgresql://user:pass@host.neon.tech/db?sslmode=require
TMDB_API_KEY=your_tmdb_v3_key
INGEST_S3_BUCKET=stream-catalog-ingest
INGEST_S3_PREFIX=deliveries
ASPERA_DROP_PREFIX=inbound
INGEST_OPERATOR_TOKEN=change-me

# AWS region
AWS_REGION=us-east-1
